name: Repair visit strategy wiring

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'One-off repair completed; workflow disabled.'
